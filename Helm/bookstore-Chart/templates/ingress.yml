apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookstore
spec:
  ingressClassName: nginx
  rules:
    - host: bookstore.local
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: bookstore
              port:
                number: 80
